import type { Loot, SpoilerLevel, Step } from '../types/guide';
import { StepList } from './StepList';
export function ChecklistSection({ steps, loot, checkedSteps, checkedLoot, mode, toggleStep, toggleLoot }: { steps: Step[]; loot: Loot[]; checkedSteps: Record<string, boolean>; checkedLoot: Record<string, boolean>; mode: SpoilerLevel; toggleStep: (id: string) => void; toggleLoot: (id: string) => void }) { return <><h2>Walkthrough</h2><StepList steps={steps} checked={checkedSteps} mode={mode} toggle={toggleStep}/><section><h2>Loot</h2>{loot.map((item) => <label className="loot" key={item.id}><input type="checkbox" checked={!!checkedLoot[item.id]} onChange={() => toggleLoot(item.id)}/><span><b>{item.name}</b> — {item.location} {item.missable && <em>Missable</em>}<small>{item.notes}</small></span></label>)}</section></>; }
