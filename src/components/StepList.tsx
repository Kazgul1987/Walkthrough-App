import type { Step, SpoilerLevel } from '../types/guide'; import { SpoilerBlock } from './SpoilerBlock';
export function StepList({ steps, checked, mode, toggle }: { steps: Step[]; checked: Record<string, boolean>; mode: SpoilerLevel; toggle: (id: string) => void }) { return <ol className="steps">{steps.map((step) => <li key={step.id}><SpoilerBlock level={step.spoilerLevel} mode={mode}><label><input type="checkbox" checked={!!checked[step.id]} onChange={() => toggle(step.id)} /><span>{step.text} {step.optional && <em>Optional</em>}{step.reward && <small>Reward: {step.reward}</small>}</span></label></SpoilerBlock></li>)}</ol>; }
